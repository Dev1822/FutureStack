/**
 * Feature flags — flip when a capability is ready for production UI.
 */

/** Set true when BYOK + pipeline are fully wired and tested end-to-end. */
export const AI_RESUME_CHECK_ENABLED = false;

export const HIRING_AGENT_REPO_URL = 'https://github.com/interviewstreet/hiring-agent';
